陷阱：
1. 缺乏项目的全局观，重复生成功能函数。比如：不同模块或cpp中的计算MD5，str2wstr等函数。
2. 缺乏实现的一致性。比如：在有log系统的情况，会使用直接print输出
3. 功能实现自由。比如：缺少规范的情况下，AI自由发挥，递归？for循环？

擅长：
1. 功能函数，功能模块
2. 擅长模仿，不会创新。所以会提高新手的工作效率，但对高手的创新业务就是灾难。
3. 擅长测试，对于有严格规范和清晰路径的文档，可以设计各种边界，各种场景。

本质：
插值引擎，AB功能之间插值，它能自学新的API调用，改写函数从C++改成python。